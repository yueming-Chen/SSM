import { inArray } from "./inArray";

export function falsy(val: any) {
  if (typeof (val) == "string" && inArray(val, ["false", "null", "undefined", "0", "NaN"])) {
    return false;
  } else {
    return !!val;
  }
}
