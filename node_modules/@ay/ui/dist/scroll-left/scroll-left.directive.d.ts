import { ElementRef, EventEmitter } from "@angular/core";
export declare class ScrollLeftDirective {
    private ref;
    private enable;
    constructor(ref: ElementRef);
    scrollChange: EventEmitter<any>;
    scrollLeft: number;
}
