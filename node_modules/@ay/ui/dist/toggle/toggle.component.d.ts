import { EventEmitter, ElementRef, TemplateRef } from '@angular/core';
export declare class ToggleComponent {
    /** data */
    private _data;
    dataChange: EventEmitter<boolean>;
    data: boolean;
    /** isDisable */
    private _isDisable;
    isDisableChange: EventEmitter<boolean>;
    isDisable: boolean;
    /** icon */
    private _icon;
    iconChange: EventEmitter<string>;
    icon: string;
    defaultIconTemplate: ElementRef;
    iconTemplate: TemplateRef<any>;
    onColor: string;
    offColor: string;
    constructor();
    toggle(): void;
    ngAfterContentInit(): void;
}
