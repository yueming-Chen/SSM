import { ElementRef, TemplateRef, ViewContainerRef, Renderer, ComponentFactoryResolver, Injector, EventEmitter, NgZone } from '@angular/core';
import 'rxjs/add/operator/take';
import { Direction } from './placements';
export declare class TooltipDirective {
    private element;
    private viewContainer;
    private injector;
    private ngZone;
    private renderer;
    behavior: "hover" | "click" | "focus";
    tooltip: string | TemplateRef<any>;
    direction: Direction;
    delay: any | any[];
    open: boolean;
    toggled: EventEmitter<boolean>;
    private popover;
    private popoverFactory;
    private componentRef;
    private viewRef;
    private _direction;
    private tether;
    private openDelay;
    private closeDelay;
    private toggleTimeout;
    private interactiveSubscription;
    onMouseClick($event: MouseEvent): void;
    onWindowsClick(): void;
    onMouseOver(): void;
    onMouseOut(): void;
    onFocus(): void;
    onBlur(): void;
    constructor(element: ElementRef, viewContainer: ViewContainerRef, injector: Injector, ngZone: NgZone, renderer: Renderer, componentFactoryResolver: ComponentFactoryResolver);
    position(async?: boolean): void;
    ngOnDestroy(): void;
    private toggle(open, delay);
    private setTether(create?);
    private setPopover();
    private create();
    private getProjectableNodes();
    private destroy();
}
