import { ElementRef, EventEmitter } from "@angular/core";
export declare class ScrollTopDirective {
    private ref;
    private enable;
    constructor(ref: ElementRef);
    scrollChange: EventEmitter<{}>;
    private _scrollTop;
    scrollTop: number;
}
