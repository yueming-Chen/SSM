import { EventEmitter, Renderer, ElementRef } from "@angular/core";
import { LoadingService } from "./loading.service";
export declare class LoadingComponent {
    loadingService: LoadingService;
    element: ElementRef;
    renderer: Renderer;
    color: string;
    height: string;
    /** progress */
    private _progress;
    progressChange: EventEmitter<number>;
    progress: number;
    /** complete */
    private _isComplete;
    completeChange: EventEmitter<boolean>;
    isComplete: boolean;
    complete(): void;
    constructor(loadingService: LoadingService, element: ElementRef, renderer: Renderer);
    ngAfterContentInit(): void;
}
